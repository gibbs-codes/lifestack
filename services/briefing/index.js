/**
 * Briefing Service - Main Entry Point
 * Generates and prints morning briefings by aggregating data from multiple services
 */

const router = require('./routes');

module.exports = {
  router
};
